    addi x1,x0,1            # Inicializa t0 con 1 (contador)
    addi x2,x0,10           # Inicializa t1 con 10 (límite superior)
    addi x3,x0, 0            # Inicializa t2 con 0 (acumulador)
    add x3, x3, x1     # t2 = t2 + t0 (acumula)
    addi x1, x1, 1      # t0 = t0 + 1 (incrementa contador)
    bne x1, x2, loop    # Si t0 != t1, salta a "loop"
